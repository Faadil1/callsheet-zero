# Required for a real live AI run (default model below is OpenAI)
OPENAI_API_KEY=

# Optional: change to another Mozaik-supported model/provider.
MOZAIK_MODEL=gpt-5.4
# ANTHROPIC_API_KEY=
# GEMINI_API_KEY=

# Optional but strongly recommended for the hackathon demo.
# Pair with: npx @mozaik-ai/cloud-sdk pair
MOZAIK_API_KEY=
